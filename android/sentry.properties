# No auth token here on purpose. sentry-cli, the Gradle integration and the
# Xcode build phase all fall back to SENTRY_AUTH_TOKEN from the environment,
# so export it locally before a release build. CI injects it from secrets.

defaults.org=ecency
defaults.project=ecency-mobile

defaults.url=https://sentry.io/
